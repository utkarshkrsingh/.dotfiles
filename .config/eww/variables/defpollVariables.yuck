(defpoll batteryIcon :interval "10s"
    "./scripts/getBatteryCharge.sh"
)

(defpoll currentBrightness :interval "0.1s"
    "./scripts/getBrightness.sh"
)

(defpoll currentDate
    :interval "60s"
    `date +"%a, %b, %d, %y"`
)

(defpoll currentTime
    :interval "1s"
    `date +%I:%M:%p`
)

(defpoll currentVolume  :interval "0.1s"
    "./scripts/getVolume.sh"
)

(defpoll currentVolumeIcon  :interval "0.1s"
    "./scripts/getVolumeIcon.sh"
)
